Location to configure:

1. config.json:

" mmp": replace with the parameters file in your genelearn path

2. env.sh:

replace with corresponding path in the installation in your environment

3. single_taxon_training.sh

replace with the env.sh path in your genelearn path

